{
  "name": "nonary-ternary-bridge-base-converter",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun test",
    "build:app": "bun build ./src/app.ts --outfile ./js/app.js --minify",
    "build:sw": "bun build ./src/sw.ts --outfile ./sw.js --minify",
    "build": "bun run build:app && bun run build:sw",
    "dev": "bun build ./src/app.ts --outfile ./js/app.js --watch"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}